# Optional: API base URL for the frontend (e.g. when testing locally against deployed API).
# Leave empty in production so the app uses relative /api (same origin).
# VITE_API_URL=https://your-app.vercel.app

# Backend only (Vercel Environment Variables or local when running `vercel dev`):
# POSTGRES_URL=postgresql://...
